services:
  garmin-tools:
    build: .
    container_name: garmin-tools-kit
    volumes:
      - ./client:/app/client
      - ./models:/app/models
      - ./data:/app/data
      - ./example:/app/example
      - ./test_data:/app/test_data
      - ./tests:/app/tests
      - ./.garth:/app/.garth
      - ./garmin_tools.py:/app/garmin_tools.py
      - ./requirements.txt:/app/requirements.txt
      - ./GEMINI.md:/app/GEMINI.md
    environment:
      - TZ=Asia/Taipei
    restart: always
    stdin_open: true          # 保持交互式介面
    tty: true                 # 分配 TTY
    working_dir: /app
